version: "3"

services:
  app:
    build:
      context: .
    user: root
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python3 manage.py runserver 0.0.0.0:8000"
